<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--圆形头像-->
    <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>

    <declare-styleable name="CircleClipLayout">
        <!-- 加个app前缀, 否则名称可能和系统或其他库冲突 -->
        <!-- 描边颜色 -->
        <attr name="app_borderColor" format="color" />
        <!-- 描边宽度 -->
        <attr name="app_borderWidth" format="dimension" />
        <!-- 裁剪内容后添加阴影 -->
        <attr name="app_shadow" format="boolean" />
    </declare-styleable>

    <!--自定义进度条-->
    <declare-styleable name="CustomProgressBar">
        <attr name="progressColor" format="color"/>
        <attr name="progressDescColor" format="color"/>
        <attr name="progressRadius" format="dimension"/>
        <attr name="max" format="integer"/>
        <attr name="progress" format="integer"/>
        <attr name="ProgressBar_borderWidth" format="dimension"/>
        <attr name="progressDesc" format="string"/>
        <attr name="isShowDesc" format="boolean"/>
    </declare-styleable>

</resources>